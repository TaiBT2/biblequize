# AUDIT_UNDOCUMENTED.md — Phase 1 Step 1.4

**Generated:** 2026-05-09
**Method:** Walk Flyway V20+ migrations + grep code; check if SPEC_USER_v3 / SPEC_ADMIN_v3 / SPEC_GROUP_v1.1 mentions feature.

---

## Features shipped but absent from current specs

| # | Feature | Evidence | In spec? | Notes |
|---|---|---|---|---|
| 1 | **Daily Mission** | V23 `add_daily_mission_table.sql`; `DailyMissionService`, `DailyMissionsCard.tsx`, `DailyMissionsWidget.tsx` | ❌ NOT mentioned | 3 micro-quests/day, auto-generated by tier. Schema: daily_missions(mission_type, config JSON, progress, target, completed, bonus_claimed). Endpoint `GET /api/daily-missions`. |
| 2 | **Tier Progress + Star popup + Milestone Burst (XP surge)** | V24 `add_xp_surge_to_users.sql`; `User.xp_surge_until`; `StarPopup.tsx`, `MilestoneBanner.tsx`, `TierProgressBar.tsx`, `TierUpModal.tsx` | ❌ NOT mentioned | Migration comment: "Milestone Burst: XP surge for 2 hours when user hits 90% tier progress." **Field defined but bonus not wired in scoring** — see BACKLOG. |
| 3 | **Comeback Bridge** | V25 `add_comeback_fields_to_users.sql`; User.last_active_date + comeback_claimed_at; `ComebackModal.tsx`; `ComebackService` | ❌ NOT mentioned | Tracks comeback reward eligibility (gap > N days → bonus). Spec defers to v? but code shipped. |
| 4 | **Tier Cosmetics (Frames + Themes)** | V26 `add_user_cosmetics_table.sql`; `user_cosmetics(unlocked_frames, active_frame, unlocked_themes, active_theme JSON)`; `Cosmetics.tsx`; `CosmeticService` | ❌ NOT mentioned | Avatar frames + quiz themes per tier; full system shipped. |
| 5 | **Prestige System (XP reset, keep cosmetics)** | V27 `add_prestige_fields_to_users.sql`; User.prestige_level, prestige_at JSON, days_at_tier6, tier6_reached_at; `PrestigeService` | ❌ NOT mentioned | Tier 6 prestige unlock after N days at tier 6; resets XP, keeps cosmetics. Zero spec coverage. |
| 6 | **Lifeline System (HINT only v1; ASK_OPINION deferred v2)** | V28 `add_lifeline_system.sql`; `lifeline_types(type='HINT'|'ASK_OPINION')`, `lifeline_usages(eliminated_option_index)`; `LifelineService`, `HintAlgorithmService`, `useLifeline.ts`. API `POST /api/sessions/{id}/lifeline/hint` + `GET /api/sessions/{id}/lifeline/status` | ⚠️ Endpoints documented in CLAUDE.md, NOT in SPEC_USER | Eliminate-1-wrong-option mechanic. Quota per session. |
| 7 | **Early Ranked Unlock** | V29 `add_early_ranked_unlock.sql` + V30 `add_early_ranked_unlocked_at.sql`; User.early_ranked_unlock + practice_correct_count + practice_total_count; admin page `EarlyUnlockMetrics.tsx` | ❌ NOT mentioned | Tier-1 users with ≥80% accuracy over 10+ Practice answers unlock Ranked early (bypass 1k XP gate). DECISIONS.md mentions; spec misses. |
| 8 | **Basic Quiz (Catechism gate to Ranked)** | V31 `add_basic_quiz_unlock.sql`; User.basic_quiz_passed; `BasicQuiz.tsx`, `BasicQuizService`, `BasicQuizController` | ❌ NOT mentioned | Pass 8/10 catechism questions to unlock Ranked mode. Question.category column added. |
| 9 | **Question Sets (User-Created, distinct from Group Quiz Sets)** | V35 `question_sets.sql`; question_sets + question_set_items; rooms.question_set_id FK; `MySets.tsx`, `SetEditor.tsx` | ❌ NOT mentioned | Personal question collections (PRIVATE/PUBLIC). Spec only mentions Group Quiz Sets, not personal ones. |
| 10 | **Custom Question IDs in rooms** | V36 `room_custom_question_ids.sql`; rooms.custom_question_ids JSON | ❌ NOT mentioned | Allows host to specify custom question list for a room. |
| 11 | **Daily Completions table** | V38 `add_daily_completions.sql`; daily_completions; `DailyCompletion` entity | ⚠️ Daily Challenge mentioned but completion tracking not specified | Tracks per-user daily challenge completion. |
| 12 | **Group Live Sequential Mode (5th room mode)** | V39 `add_group_live_sequential_room_mode.sql`; SequentialScoringService; `/advance` STOMP handler | ✅ SPEC_GROUP §8 | Documented in group spec; but USER spec only lists 4 multiplayer modes — needs cross-ref. |
| 13 | **Scheduled Quizzes (group)** | V40 `scheduled_quizzes.sql`; ScheduledQuizService; `ScheduledQuizCreate.tsx`, `ScheduledQuizDetail.tsx`, `ScheduledQuizPlay.tsx` | ✅ SPEC_GROUP §9 | Documented in group spec. |
| 14 | **Group Kick Logs + Reports** | V41 `group_kick_log_and_reports.sql`; GroupKickLog, GroupReport entities | ⚠️ Partially | Group spec mentions kick; report flow needs verification. |
| 15 | **Admin Test Panel (dev/staging)** | AdminTestController + SetStateRequest, SetMissionStateRequest, SeedPointsRequest DTOs; `TestPanel.tsx` | ✅ SPEC_ADMIN §17 | Correctly specified. |
| 16 | **Mobile Auth (3 endpoints)** | `MobileAuthController`: POST /api/mobile/auth/google, POST /api/mobile/auth/refresh | ❌ NOT mentioned | Mobile-specific OAuth flow (different token lifetime, no httpOnly cookie). |
| 17 | **i18n validator** | `apps/web/scripts/validate-i18n.ts`; `npm run validate:i18n` | ⚠️ Mentioned in CLAUDE.md, not specs | Coverage tooling. |
| 18 | **Question search indexes (perf)** | V22 `add_question_search_indexes.sql` | ❌ NOT in spec (infrastructure detail) | OK to omit from user spec; mention in admin spec for question CRUD perf context. |
| 19 | **Group lock fields** | V19 `add_group_lock_fields.sql`; church_groups.locked_at | ⚠️ AdminGroupController has POST /{id}/lock; not in spec | Admin lock flow needs spec. |
| 20 | **Group analytics (leader-only)** | `GroupAnalytics.tsx` page + endpoints | ❌ NOT mentioned | Leader dashboard for group stats. |
| 21 | **Question Quality admin page** | `QuestionQuality.tsx` admin page | ❌ NOT mentioned | Admin metric for question quality scores. |
| 22 | **ExportCenter admin page** | `ExportCenter.tsx` | ❌ NOT mentioned | Data export tool for admin. |
| 23 | **Notification Campaign builder** | admin `Notifications.tsx` | ⚠️ Notif endpoints documented; campaign builder not | Admin can broadcast notifications. |
| 24 | **Activity Feed component** | `ActivityFeed.tsx`, `LiveFeed.tsx` | ❌ NOT mentioned | Home recent activity ticker. |
| 25 | **Daily Verse banner** | `DailyVerseBanner.tsx` + `DailyThemeService` | ❌ NOT mentioned | Verse-of-the-day rotation. |
| 26 | **Tutorial Overlay** | `TutorialOverlay.tsx` | ❌ NOT mentioned | Interactive onboarding overlay. |
| 27 | **Bookmark questions** | Bookmark entity + endpoint | ❌ NOT mentioned | Save question for later review. |
| 28 | **Room Cleanup hardening (R1-R5)** | V45-V48 + RoomCleanupScheduler + RoomAbandonmentScheduler + PresenceTracker | ⚠️ R1-R5 lifecycle is canonical (C7) but specs don't yet enumerate them | Phase 2 spec should explicitly document R1-R5. |
| 29 | **Streak notification dedup** | V47 `dedup_streak_notifications.sql` | ❌ NOT (infrastructure) | OK to omit. |
| 30 | **Orphan practice session purge** | V46 `abandon_orphan_practice_sessions.sql` | ❌ NOT (infrastructure) | OK to omit. |

---

## Tooling / infra not in specs (lower priority)
- TanStack Query (mentioned but unclear adoption)
- Sentry monitoring (configured but not active)
- Test data seeder endpoint

---

## Summary

**~25 user-facing features shipped but not in SPEC_USER_v3 / SPEC_ADMIN_v3.** Phase 2 must document every row marked ❌ in the table above.
